# Copy to .env and adjust for your host ( .env is gitignored ).
#
# macOS (Docker Desktop):
DEVCONTAINER_SSH_AUTH_SOCK=/run/host-services/ssh-auth.sock
#
# Linux (use your host agent socket path; echo $SSH_AUTH_SOCK):
# DEVCONTAINER_SSH_AUTH_SOCK=/tmp/ssh-XXXX/agent.12345
