{
  "name": "ros-livekit-bridge",
  "build": {
    "context": "..",
    "dockerfile": "../Dockerfile",
    "args": {
      "WS_ROS": "livekit_ws"
    }
  },
  "workspaceFolder": "/livekit_ws",
  "workspaceMount": "source=${localWorkspaceFolder},target=/livekit_ws,type=bind",
  "mounts": [
    "source=${localEnv:DEVCONTAINER_SSH_AUTH_SOCK:/run/host-services/ssh-auth.sock},target=/ssh-agent,type=bind"
  ],
  "remoteUser": "root",
  "init": true,
  "privileged": true,
  "runArgs": [
    "--ipc=host",
    "--network=host"
  ],
  "containerEnv": {
    "DISPLAY": "${localEnv:DISPLAY}",
    "QT_X11_NO_MITSHM": "1",
    "CMAKE_EXPORT_COMPILE_COMMANDS": "1",
    "SSH_AUTH_SOCK": "/ssh-agent"
  },
  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  },
  "postCreateCommand": "bash -lc 'source /opt/ros/jazzy/setup.bash && mkdir -p src/externals && if [ -f external.repos ]; then vcs import --skip-existing src/externals < external.repos; fi && rosdep update && rosdep install --from-paths src/ros2_livekit_bridge --ignore-src -r -y'",
  "postStartCommand": "bash -lc 'if [ -d src/externals ]; then for repo in src/externals/*; do if [ -d \"$repo/.git\" ]; then if [ -n \"$(git -C \"$repo\" status --porcelain)\" ]; then echo \"Skipping pull for $repo because it has local changes\"; elif ! git -C \"$repo\" symbolic-ref --quiet --short HEAD >/dev/null; then echo \"Skipping pull for $repo because it is detached HEAD\"; else git -C \"$repo\" pull --recurse-submodules; fi; fi; done; fi'",
  "customizations": {
    "vscode": {
      "extensions": [
        "llvm-vs-code-extensions.vscode-clangd",
        "ms-vscode.cmake-tools",
        "redhat.vscode-yaml"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "/bin/bash",
            "args": ["-l"]
          },
          "zsh": {
            "path": "/usr/bin/zsh"
          }
        },
        "clangd.path": "/usr/bin/clangd",
        "C_Cpp.intelliSenseEngine": "disabled"
      }
    }
  }
}
