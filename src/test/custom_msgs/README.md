# Custom Messages

This test package represents user custom messages, ensuring the bridge can
support them via integration tests.

Run the sample publisher with:

```bash
ros2 run custom_msgs robot_status_publisher
```

## Schemas

See [here](./schema/RobotMsgSchema.txt) for an example schema rendering of the custom message.